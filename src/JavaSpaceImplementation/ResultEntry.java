// Result Entry class for JavaSpaces
package JavaSpaceImplementation;

import net.jini.core.entry.Entry;
import DataSpaceElements.Result;

public class ResultEntry extends Result implements Entry{
    public ResultEntry() {
    }
}
