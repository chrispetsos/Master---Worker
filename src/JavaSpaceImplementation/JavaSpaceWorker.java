package JavaSpaceImplementation;

import MasterWorkerSystem.Worker;

public class JavaSpaceWorker {
    public static void main(String[] args) {
        Worker worker = new Worker();
        worker.assignSpace(new Javaspace());
    }
}
